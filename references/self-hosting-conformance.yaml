# Self-hosting conformance manifest — content-presence guard for THIS repo.
#
# This repo is self-hosting: formal docs live at the root (SKILL.md,
# WORKFLOW.md, TEMPLATES-GUIDE.md, references/*) instead of under
# skills/<name>/workflows/. The downstream-facing manifest lives at
# `templates/skill/conformance.yaml` and assumes the standard layout, so it
# cannot validate this repo's own canonical content.
#
# This file fills that gap: assert that the upstream still teaches the
# protocols, gates, and principles its own templates promise. If a refactor
# accidentally drops "Task Closure Protocol" from TEMPLATES-GUIDE.md or
# "Progressive Rigor" from SKILL.md, the next `scripts/check-all.sh` run fails.
#
# Validate with:
#   bash templates/skill/scripts/check-version-conformance.sh . \
#     --conformance references/self-hosting-conformance.yaml
#
# Bumping rule: when you add a new mandatory protocol/principle phrase to
# `templates/skill/conformance.yaml`, mirror it here against whichever
# self-hosting file teaches the same thing. Otherwise upstream can quietly
# stop teaching what the templates require downstream to carry.

required_sections:
  - file: README.md
    must_contain:
      - "SBA is a Skill, not an agent operating system, task database, or execution runtime"
      - "derives an evidence-selected `direct`, `folder`, or `broad` result"
      - "does not expose a tier/profile/capability/install-mode switch"
      - "source-to-destination semantic evidence"
      - "fitted checks that the materializer emitted"
      - "Sub-agents are optional accelerators, not a setup requirement"
      - "continues the same bounded work inline"
      - "Correctness and completion never depend on sub-agent availability"
      - "changes the execution mode, not the requested result"
      - "Structural checks do not prove that every previous instruction survived semantically"
      - "a pushed branch is not an MR or PR"
    must_not_contain:
      - "Codex is the exception"
      - "you have to authorize it explicitly"
      - "silently skip the delegation step"
      - "planned evidence-driven materializer remains roadmap work"
      - "not a shipped capability"

  - file: README.zh-CN.md
    must_contain:
      - "SBA 是一个 Skill，不是 Agent 操作系统、任务数据库或执行 runtime"
      - "按证据推导 `direct`、`folder` 或 `broad` 结果"
      - "不向用户暴露 tier/profile/capability/安装模式开关"
      - "source 到 destination 的语义证据"
      - "只运行 materializer 实际生成的 fitted checks"
      - "Sub-agent 是可选加速器，不是安装或正常使用的前置条件"
      - "正确性和完成标准不依赖 sub-agent 是否可用"
      - "inline 执行同一份有界工作"
      - "只会改变执行方式，不会改变请求结果"
      - "结构检查不能证明每一条旧规则都完成了语义迁移"
      - "已 push 的分支不等于 MR/PR"
    must_not_contain:
      - "Codex 是例外"
      - "都要**显式授权**"
      - "静默跳过委派步骤"
      - "仍是 roadmap 工作"
      - "不是已经交付的能力"

  # SKILL.md teaches the activation contract — every principle below has a
  # downstream counterpart that propagates via templates/skill/SKILL.md.template.
  - file: SKILL.md
    must_contain:
      - "## Progressive Rigor"
      - "Routing.yaml as source"
      - "Activation over storage"
      - "every loaded file needs an independent task-time reason"
      - "Self-maintenance"
      - "Token efficiency"
      - "Task Execution + Closure"
      - "Present alignment proportionally"
      - "Anchor Checkpoint"

  # TEMPLATES-GUIDE.md after the 2026-05-09 slim only carries Minimal Starter,
  # Classification Guide, Sync Targets, and pointers to canonical workflow
  # templates. The protocol-level assertions previously held here moved to
  # references/protocols.md (concepts) and templates/skill/workflows/update-rules.md
  # (the workflow text itself, validated by templates/skill/conformance.yaml).
  # We still assert TEMPLATES-GUIDE.md keeps its core scaffolding role.
  - file: TEMPLATES-GUIDE.md
    must_contain:
      - "## Minimal Starter Template"
      - "## Classification Guide"
      - "## Sync Targets"
      - "## Task Execution Protocol"
      - "## Task Closure Protocol"
      - "task-execution.md"
      - "Anchor Checkpoint"
      - "plan-large.md"
      - "subagent-auxiliary.md"
      - "subagent-driven.md"
      - "subagent-orchestration.md"
      - "Task Ref/Role dispatch"
      - "vendor/project-owned/generated/runtime-data owners"

  - file: docs/sba-bible.md
    must_contain:
      - "## 产品边界"
      - "SBA 就是一个 Skill，不是 Agent 操作系统"
      - "使用所在 harness 已有的 Plan、Subagent 和工具能力"
      - "不自建常驻服务、任务数据库、统一状态机或独立调度 runtime"
      - "## 最高纲领"
      - "## 要证明的三件事"
      - "看到更完整的信息"
      - "在没有标准答案时做判断"
      - "让别人按照你的意愿做事情"
      - "## 真正的不可替代"
      - "定义标准、判断风险、协调资源、带出结果"
      - "新机制必须减少责任或解决真实失败，不能只是让架构显得完整"
      - "默认核心应该更小但完整"
      - "规则必须能被正确激活；仅仅存进仓库不算产生价值"
      - "Agent 的结论必须可复核"
      - "语义完整性优先于代码改动最少，但验证成本必须与风险匹配"
      - "验证前必须把风险、验证证据和停止条件绑定起来"
      - "不得用测试数量代替证据质量"
      - "跨 harness 兼容应当优雅降级"
      - "SBA 负责吸收工程复杂度"
      - "Agent 的完成承诺只覆盖当前任务明确绑定的交付物及其请求终态"
      - "历史任务、相邻仓库和隐含后续流程"
      - "不得用中间状态冒充目标终态"
      - "需求是根，是目标与验收契约；Plan 从需求长出来，只拥有达成它的实现细节"
      - "清晰原话可以零问题达到 `requirement-ready`"
      - "验收标准属于需求，红测、自动化测试、浏览器/运行读回与人工确认只是 Plan 选择的证明方式"
      - "正确顺序是 `requirement-ready -> Task Anchor -> implementation-ready -> Native Plan -> mutation`"
      - "逻辑分离是强制的，文件分离不是"
      - "产品语义 → 可执行变更契约 → 当前代码 owner"
      - "语义翻译与实现绑定由同一运行时 Change Contract 贯通"
      - "文件、类、搜索词、当前 owner 等 Agent 能从仓库获得的技术事实不得转交用户"
      - "PRD、Figma、关键词、任务大小或固定流程"
      - "直接复用并走快路径"
      - "用户原话先于 Agent 提炼"
      - "已经清楚的原话应直接保留"
      - "不得让提炼稿成为唯一的规范记录"
      - "Agent 负责目标、语义、参数选择、异常解释和升级判断"
      - "低自由度执行只在真实压力下下沉"
      - "一次性、低风险、可直接验证的动作不因“可以写脚本”就被固化"
      - "确定性载体只拥有声明的输入、执行、稳定输出/错误与幂等契约"
      - "不发展成 Agent 操作系统、任务数据库或项目管理平台"
      - "不为了“以后也许有用”持续增加文件、状态、marker、脚本和术语"
      - "不用结构检查的绿色结果冒充语义正确"
      - "不因为外部项目设计漂亮就直接吸收"
      - "新证据推翻承重结论时，不得静默覆盖"
      - "涉及规范性业务判断时，由业务 owner 确认"
      - "代码可以暴露约束、成本和风险，但无权替用户修改已经确认的业务主线"
      - "发现偏移必须立即停下来协商"
      - "触发条件、立即动作、当前任务的验收责任可以适度重复"
      - "完整定义、状态转换和多步骤流程原则上只维护一次"
      - "完整重复比跳转、加载或生成更便宜"
      - "不得机械地“一律抽取”"
      - "主步骤必须产生能够改变下一步判断或被下一阶段消费的证据"
      - "执行过命令、生成了文件或获得成功退出码，本身不构成阶段完成"
      - "Plan 的逻辑覆盖与物理材料化必须分离"
      - "先区分用户要形成/更新具体 Plan"
      - "后者只取足以支持判断的代表性证据，不执行完整规划"
      - "复杂度只决定分析深度，不决定是否创建文件"
      - "证据一旦能够解决或推翻该问题，或定位下一个语义 owner，就停止扩读"
      - "不能自动变成整仓读取义务"
      - "已有 Plan 存在多个 Open Questions 时，先选择影响最大的一个"
      - "其余问题和证据路径在轮到之前保持未打开"
      - "必须先讨论这一件事，再打开无关 Slice"
      - "只有用户明确要求一次性完整产物时才连续展开"
      - "只有用户明确需要持久产物"
      - "模块间冲突等真实承载压力"
      - "首个文件与每个后续 sibling 都必须独立证明"
      - "第一次已经证明、且能改变未来动作的失败经验"
      - "立即写回正确 owner 并接入下一次动作"
      - "不等待第二次复发，不拖到最终 AAR"
      - "没有命中就修激活，命中后仍失败才在现有权限边界内加强可执行预防"
      - "复发本身不扩大提交、推送、MR、部署、数据库、团队规则或外部系统权限"
      - "完整流程只有在至少两次独立完成后"
      - "身份来自共同目标、有序阶段、输入输出、集成验收和权限关卡"
      - "纯组合没有持久缺口时不提案"
      - "提案必须排在 `Closure Complete` 的结果报告之后"
      - "沿真实 provenance 只修改 canonical owner，并完成下游装配/安装与验证"
      - "Workflow 接受不扩大提交、推送、MR、部署、数据库、工单、版本、评审人、发布或合并权限"
      - "## 方向决策检查"
      - "它不进入下游模板"

  # WORKFLOW.md after the 2026-05-09 split is the Quick Start entry; the full
  # 9-phase content (including Phase 9 pressure-test) moved to
  # workflows/full-migration.md. Assert WORKFLOW.md still routes to the new
  # locations and full-migration carries the protocol anchors.
  - file: WORKFLOW.md
    must_contain:
      - "Quick Start"
      - "workflows/full-migration.md"
      - "templates/skill/workflows/update-upstream.md"
      - "scripts/scaffold-downstream.sh"
      - "never overwrites existing project entry files"
      - "source-to-destination evidence"
      - "does **not** prove that pre-existing user instructions survived"
      - "task-execution.md#user-decision-drift-gate"
      - "Begin with a read-only code/config/rule scan"
      - "For an ordinary request such as `用 SBA 整理这个项目的规则`"
      - "Ask only when evidence cannot resolve a normative product/business decision"
      - "continue directly to preview and apply"
      - 'MIGRATION_EVIDENCE_JSON="$(mktemp'
      - "The path is not a persistent downstream ledger"
    must_not_contain:
      - "Start with the workflow's user brainstorm gate"
      - "do not read code yet"

  - file: templates/skill/workflows/profile-project.md
    must_contain:
      - "Do not begin by asking whether the user wants to brainstorm"
      - "complete the read-only evidence scan first"
      - "Never ask the user for an implementation fact or technical destination"
      - "User-confirmed desired truth"
      - "rules require `Confirmed from code/config`; normative rules may use faithfully"
      - "preserved `User-confirmed desired truth` with its question/context and provenance"
      - "apply the admitted executable-architecture contract"
      - "only when low freedom and repeated/high-error pressure both exist"
      - "prefer a structured API, existing CLI, existing script, then a new minimal script"
      - "Keep goals, semantic parameters, exceptions, authority, and escalation with the Agent"
      - "keep a one-off directly verifiable action on the direct path"
      - "authoritative re-readable result"
      - "run identity, provenance, freshness, completeness/semantic validity, and terminal state"
    must_not_contain:
      - "User-calibrated"
      - "../../../references/executable-skill-architecture.md"

  - file: references/thin-shells.md
    must_contain:
      - "Request-clarity judgment"
      - "vague wording is a signal, not an automatic blocker"
      - "bounded read-only project evidence"
      - "Do not mutate before the outcome is verifiable"
      - "Before any requested commit/push/MR/deploy delivery"
      - "Ready for Delivery` is not completion"

  - file: workflows/full-migration.md
    must_contain:
      - "Phase 9"
      - "Task Execution Protocol"
      - "Task Closure Protocol"
      - "After-Action Review"

  # protocols.md is the cross-skill reference for execution + closure. It must
  # carry the same canonical headings the templates require downstream.
  - file: references/protocols.md
    must_contain:
      - "## Task Execution Protocol"
      - "Workflow owns the reusable domain procedure"
      - "not a fixed chat template"
      - "visible native Plan for step display"
      - "Anchor Checkpoint"
      - "does not duplicate either gate's complete procedure"
      - "## Task Closure Protocol"
      - "## Verified Failure Learning"
      - "first proven actionable occurrence does not wait for a second occurrence"
      - "creates no candidate ledger, counter, chronology, Memory route, or database"
      - "Judge two independent failure classes"
      - "**Activation failure** means applicable prevention is already stored"
      - "**Learning-closure failure** means the first proven stable prevention"
      - "Evidence cannot substitute across the boundary"
      - "Repair missing/inert activation before adding prose"
      - "## Proactive Workflow Distillation"
      - "Two independently completed semantic instances may justify one optional result-first question"
      - "stay silent for pure composition"
      - "verify every affected downstream consumer"
      - "## Recording Threshold"
      - "## Generalization Rule"
      - "fidelity"
      - "reconciliation"
      - "cross-implementation stability"
      - "bind material risk to fitted evidence and a stop/escalation condition"
      - "user-confirmed"
      - "Plan implementation handoff"
      - "current implementation fact"

  - file: references/business-global-model.md
    must_contain:
      - "## Orthogonal task and domain routing"
      - "routing.yaml` selects exactly one first workflow"
      - "domain-routing.yaml"
      - "domain_overlays"
      - "task_route_id"
      - "Keywords identify candidates only"
      - "Clearly technical work never reads it"
      - "Bind one domain owner by default"
      - "## Cross-owner reads"
      - "owner_roots"
      - "owner:<owner-id>:<path>"
      - "target existence as unverified"
      - "## Provenance and retirement"
      - "not a permanent double-maintenance protocol"
      - "## Desired truth and implementation fact"
      - "canonical information-model definition"
      - "## Confirmed implementation gap intake"
      - "Standalone modeling defaults to the routed business model, not a PRD"
      - "all four conditions hold"
      - "Never create one PRD per gap"
      - "The first gap does not interrupt the modeling task"
      - "Do not ask code and the confirmed model to compete for authority"
      - "User Decision Drift Gate"
      - "task-execution.md#user-decision-drift-gate"

  - file: templates/skill/protocol-blocks/ambiguous-request-gate.md
    must_contain:
      - "## Implementation-Fact Question Gate"
      - "close the smallest decision-relevant implementation path"
      - "Trace distinct flows independently"
      - "remaining normative ambiguity"

  - file: templates/skill/rules/change-discipline.md
    must_contain:
      - "## Semantic Boundary"
      - "Default to Product Development"
      - "project-rules.md"
      - "coding-standards.md"
      - "ownership/provenance"
      - "Dependency count is risk evidence"
      - "## Mutation Boundary"

  - file: templates/skill/workflows/task-execution.md
    must_contain:
      - "## Evidence and Knowledge Gate"
      - "smallest source/runtime slice"
      - "diagnostic evidence, never correctness gates"
      - "one clear read-only or fixed-contract maintenance action"
      - "no new desired behavior/contract"
      - "a `requirement-ready` addition/change to user-visible behavior, business flow/state, or external contract"
      - "Apparent code locality never makes requirement-bearing work Simple"
      - "reclassify before more mutation"
      - "build only the remaining Native Plan rather than asking the user to restate the task"
      - "remaining Done When evidence must be explicitly delivery-dependent"
      - "Closure decides readiness/completion"
      - "cross-cutting modifier, never a first-workflow route"
      - "task size or duration alone does not activate it"
      - "## Composite Plan Handoff"
      - "composite-plan-execution.md"
      - "Components never create competing runtime anchors"
      - "## Database-Structure Mutation Gate"
      - "dedicated Plan-local SQL sibling"
      - "never permission to bypass the project migration workflow or write a database"
      - "## Verified Failure Handoff"
      - "Session-only failure candidate"
      - "Retries inside the same unresolved task remain one occurrence"
      - "rule-update/verified-failure.md#verified-failure-input"
      - "every verified failure has a Rule Update learning outcome"
      - "After `requirement-ready` and before implementation-binding discovery"
      - "After source localization makes the Change Contract `implementation-ready`"
      - "derive the Native Plan"
      - "cannot infer or edit the Requirement"
      - "Technical evidence that changes only Current facts"
      - "without editing the Anchor"
      - "returns to Requirement Definition; only after the authoritative Requirement is"
      - "updated and reaches `requirement-ready` again"
      - "Already-clear wording stays verbatim"
      - "an Agent-derived summary cannot be the only normative record"
      - "If technical evidence changes only the implementation premise or ordering"
      - "do not edit the Anchor"
      - "Requirement Definition and re-project the Anchor only after the Requirement"
    must_contain_in_order:
      - "After `requirement-ready` and before implementation-binding discovery"
      - "After source localization makes the Change Contract `implementation-ready`"
      - "derive the Native Plan"
    must_not_contain:
      - "locally_done"
      - "ready_for_parent"
      - "waiting_for_component"
      - "component_status"
      - "integration_status"
      - "plan_role"
      - "Re-anchor only when the user or evidence changes the Requirement frame"
      - "If evidence changes the premise, scope, ordering, or Done When, update the Anchor"

  - file: templates/skill/protocol-blocks/composite-plan-execution.md
    must_contain:
      - "# Composite Plan Execution"
      - "one truthful Integrating owner"
      - "one Integrating Task Anchor and one Native Plan"
      - "Session-only composition creates no durable graph artifact"

  - file: templates/skill/protocol-blocks/user-decision-drift.md
    must_contain:
      - "# User Decision Drift"
      - "Keep the affected path paused"
      - "Mark every alternative `proposed`"
      - "Source-Preserving User Input"
      - "keep clear wording verbatim"
      - "label any Agent-derived summary as subordinate"
      - "speech act/force, actor/ownership, timing/commitment, uncertainty/emphasis"
      - "in the governing Requirement mainline"
      - "mark the prior decision `superseded`"
      - "re-project the Task Anchor, rebind implementation facts, and replace the remaining Native Plan before more mutation"
      - "A late disclosure in review or Closure never substitutes"

  - file: templates/skill/workflows/define-requirement.md
    must_contain:
      - "# Requirement Definition Workflow"
      - "produces `requirement-ready` and, only under its own"
      - "Artifact Pressure Gate, the Governing Requirement artifact"
      - "sole owner of Requirement Contract dimensions, authority, readiness, and return states"
      - "## Intake And Evidence"
      - "Evaluate only the Requirement Contract dimensions activated by this request"
      - "cannot choose desired product meaning for the user"
      - "### Clear Requirement"
      - "the source-faithful Requirement Contract and return `requirement-ready`"
      - "Do not require the user to reply with a ceremonial confirmation"
      - "Keep it Session-only unless the Artifact Pressure Gate below admits a Governing"
      - "### Incomplete Requirement"
      - "present the current understanding in concrete outcome terms"
      - "ask the minimum normative question needed"
      - "stop before implementation planning, Task Breakdown, or mutation"
      - "Do not fill a gap with an \"implementation assumption\""
      - "## Brainstorm Boundary"
      - "Discuss one load-bearing normative topic at a time"
      - "material risk or trade-off, the Agent's recommendation"
      - "When the Requirement is already clear, ask no question"
      - "ordinary implementation means belong to"
      - "## Decision Authority And Source Fidelity"
      - "Keep a clear user statement verbatim as `User source`"
      - "## Requirement Artifact Pressure Gate"
      - "update the smallest Governing Requirement only when the user explicitly asks"
      - "independent reader, review"
      - "Complexity, activity, implementation size, or a clear Requirement alone is not"
      - "real project code root and follow its local Requirement archive contract"
      - "Exclude Current -> Target implementation design"
      - "Requirement does not enter Plan Feature or authorize mutation"
      - "A later durable implementation Plan links to and consumes this owner one way"
      - "Plan Feature cannot copy, narrow, resolve, or update the Governing"
      - "## Handoff"
      - "Requirement Definition does not derive implementation steps"
    must_not_contain:
      - "## Technical Design Slices"
      - "## Proof Design"
      - "Make one risk-sized change"
      - "run the repository-owned fitted validation command"
      - "Do not create a Requirement file, Task Anchor, or Plan here"
      - "Requirement-only PRD uses Plan Feature's Artifact Pressure Gate"

  - file: templates/skill/workflows/plan-feature.md
    must_contain:
      - "# Implementation Plan Workflow"
      - "not the Requirement Definition owner"
      - "## Deliverable Gate"
      - "only representative evidence and return findings/next decisions"
      - "materialize artifacts, build Task Breakdown, or mutate code"
      - "## Intake Sequence"
      - "Never close it with an implementation assumption"
      - "Project the Task Anchor"
      - "Prove implementation facts"
      - "Reach `implementation-ready`"
      - "derive technical design, Task Interfaces, proof implementation"
      - "Task Breakdown and mutation are forbidden before this state"
      - "Complexity changes analysis depth, never file creation"
      - "## Technical Design Slices"
      - "question -> Current evidence -> technical decision -> impact/risk -> proof"
      - "preserve the governing Requirement"
      - "returns to Requirement Definition"
      - "## Proof Design"
      - "The proof implementation is subordinate to acceptance"
      - "an unstated expected result, return that question to Requirement Definition"
      - "## Artifact Pressure Gate"
      - "after `implementation-ready`, the harness-native"
      - "Complexity or activity alone is not pressure"
      - "A durable Plan names and consumes its governing Requirement"
      - "An explicit request to create or update only the Requirement/PRD stays in"
      - "Requirement Definition and never borrows this Gate"
      - "return to Requirement Definition to materialize that owner first"
      - "use separate dossier carriers"
      - "`prd.md` owns the Requirement and `design.md` owns the Implementation Plan"
      - "`design.md` declares `requirement: prd.md`"
      - "A Requirement-only dossier may stop at `prd.md`"
      - "A new implementation Plan never uses `prd.md` as its technical or lifecycle owner"
      - "## Database-Change Artifact Gate"
      - "Its `prd.md` Requirement,"
      - "`design.md` Plan, and SQL surface remain separate owners"
      - "The artifact never grants database-write authority"
      - "## Plan Contract And Handoff"
      - "A durable implementation Plan is a revisable technical contract"
      - "must not redefine Goal, scope, model/flow, rules"
      - "Every proof traces to Requirement acceptance"
      - "Requirement-only artifact delivery never enters this workflow"
      - "without copying or modifying it"
      - "[workflow-state:planning]"
    must_contain_in_order:
      - "Consume the Requirement"
      - "Project the Task Anchor"
      - "Prove implementation facts"
      - "Reach `implementation-ready`"
    must_not_contain:
      - "## Question Gate"
      - "## User-Confirmed Decision Mainline"
      - "## Progressive Brainstorm Handoff"
      - "canonical owner of Decision Delta"
      - "automatically enter a detailed user-facing brainstorm"
      - "docs/sba-bible.md"
      - "or a dossier `prd.md` only when it already needs independently readable"
      - "otherwise `prd.md` or the existing one-file Plan"
      - "plan_role"
      - "consumed_by"
      - "component_status"
      - "integration_status"
      - "Requirement-only PRD uses Plan Feature's Artifact Pressure Gate"

  - file: templates/skill/workflows/plan-large.md
    must_contain:
      - "Large changes analysis depth, not artifact count"
      - "a perspective is not a file"
      - "A suspected risk or consumer list is a candidate queue"
      - "Keep tightly coupled angles together"
      - "Never create one file per lens"
      - "conversation when no artifact is justified"
      - "or `design.md` when a durable Requirement dossier exists"
      - "`prd.md` remains the Governing Requirement and never becomes the Large Plan synthesis"
      - "Do not dispatch broad “scan the subsystem” work before that boundary"
    must_not_contain:
      - "otherwise `prd.md` or the existing one-file Plan"

  - file: templates/skill/workflows/profile-business-model.md.example
    must_contain:
      - "does not create a PRD merely because the scope is long or complex"
      - "Reconcile leaf / Gotcha ownership"
      - "Implementation-Fact Question Gate"
      - "## Confirmed Implementation Gap Collection and Handoff"
      - "reuse an authoritative Governing Requirement for the same scope"
      - "separate `design.md` consumer instead of putting technical Plan content in `prd.md`"
      - "Continue the user's agreed modeling scope"
      - "Plan Feature may write implementation design only to the separate Plan carrier"
      - "keep the Requirement draft and stop"
      - "--workspace-root <workspace-root>"

  - file: templates/skill/workflows/fix-bug.md
    must_contain:
      - "Implementation-Fact Question Gate"
      - "Never ask the user to choose a Current implementation fact"
      - "If the user says the answer is in code"
      - "rule-update/verified-failure.md#verified-failure-input"
      - "For each verified engineering failure, run Verified Failure Input now"
      - "does not defer the verified-failure write"

  - file: templates/skill/workflows/update-rules.md
    must_contain:
      - "compact shared durable-write contract plus the ambiguous-request dispatcher"
      - "## Shared Durable-Write Contract"
      - "### Input and Destination Classification"
      - "rule-update/business-truth.md"
      - "rule-update/verified-failure.md"
      - "rule-update/knowledge-reconciliation.md"
      - "rule-update/workflow-distillation.md"
      - "### Source, Authority, and Fidelity"
      - "### Reconciliation Gate"
      - "### Activation Check"
      - "### Placement and Generalization"
      - "### Synchronization and Verification"
      - "## Ambiguous Request Dispatcher"
      - "Select exactly one execution owner"
    must_not_contain:
      - "## Business Leaf and Gotcha Ownership"
      - "## Verified Failure Input"
      - "## Ordinary Recording Threshold"
      - "## Accepted Workflow-Distillation Input"

  - file: templates/skill/workflows/rule-update/business-truth.md
    must_contain:
      - "## Business Leaf and Gotcha Ownership"
      - "## Requirement Decision Source"
      - "define-requirement.md#decision-authority-and-source-fidelity"
      - "change-contract.md#requirement-contract"
      - "An implementation Plan carries only one-way Requirement Provenance"
      - "## Standalone Business-Model Input"
      - "## Active Bug-Fix Input"
      - "Do not derive `desired business truth` from Bug symptoms"
      - "## Business-Specific Completion"
    must_not_contain:
      - "`covered/no-write`"
      - "## Accepted Workflow-Distillation Input"

  - file: templates/skill/workflows/rule-update/verified-failure.md
    must_contain:
      - "## Verified Failure Input"
      - "first proven actionable failure bypasses only the ordinary Recording Threshold"
      - "**Activation failure** - applicable prevention already exists"
      - "**Learning-closure failure** - the first occurrence has already proved a stable prevention"
      - "Their evidence is not interchangeable"
      - "Evaluate and close both classes separately whenever both are present"
      - "Stable execution preconditions need an earlier placement than their failure explanation"
      - "before its first protected command"
      - "count tool initialization as the intended red test"
      - "project-specific versions and commands stay downstream"
      - "same proven root cause + same applicability boundary + same prevention action"
      - "Failure learning never grants commit, push, MR, deploy, publish, database, production, or other external authority"
      - "## Failure-Specific Completion"
      - "Activation failure and learning-closure failure were evaluated independently"
      - "one correct run was not used as proof of durable learning"
    must_contain_in_order:
      - "Stable execution preconditions need an earlier placement than their failure explanation"
      - "select the environment"
      - "verify what the command will actually use"
      - "before its first protected command"
    must_not_contain:
      - "## Business Leaf and Gotcha Ownership"
      - "## Accepted Workflow-Distillation Input"

  - file: templates/skill/workflows/rule-update/knowledge-reconciliation.md
    must_contain:
      - "## Ordinary Recording Threshold"
      - "## Upgrade Plan Gate"
      - "## When Not to Record"
      - "## Learn, Correct, Retire"
      - "repair activation/prominence instead of duplicating it"
    must_not_contain:
      - "## Verified Failure Input"
      - "## Accepted Workflow-Distillation Input"

  - file: templates/skill/workflows/rule-update/workflow-distillation.md
    must_contain:
      - "## Accepted Workflow-Distillation Input"
      - "at least two independently completed semantic instances"
      - "Create one complete orchestration owner"
      - "source-only change without required downstream consumption is incomplete"
      - "generated or installed copies are never independent owners"
      - "Workflow acceptance never grants commit, push, MR, deploy, publish, database"
    must_not_contain:
      - "## Business Leaf and Gotcha Ownership"
      - "## Verified Failure Input"

  - file: templates/skill/workflows/task-closure.md
    must_contain:
      - "Account for verified failures"
      - "rule-update/verified-failure.md` § Verified Failure Input"
      - "Closure does not diagnose root cause or postpone the first write"
      - "Do not re-record an already-reconciled verified failure"
      - "### Post-Completion Workflow Distillation"
      - "workflow-distillation-proposal.md"
      - "plan-knowledge-closure.md"
      - "external-delivery-verification.md"
      - "session-only requested artifact set"
      - "every requested artifact only against that bound set"
    must_not_contain:
      - "## Accepted Workflow-Distillation Input"

  - file: templates/skill/protocol-blocks/plan-knowledge-closure.md
    must_contain:
      - "# Plan And Knowledge Closure"
      - "Replay each relevant user-confirmed Requirement Decision Context"
      - "Reconcile later Requirement Decision Deltas"
      - "Plan Leaf Reconciliation before implementation handoff"
      - "preserve one-way Requirement Provenance"
      - "consume rather than redefine that meaning"
      - "Run Knowledge Reciprocity"
      - "Calibrate `distilled_to:`"

  - file: templates/skill/protocol-blocks/external-delivery-verification.md
    must_contain:
      - "# External Delivery Verification"
      - "## Requested Artifact Set"
      - "Historical tasks, adjacent repositories or systems"
      - "create no ledger, database, or persistent status file"
      - "Before answering an aggregate status question"
      - "a pushed branch is not an MR/PR"
      - "an approved MR/PR is not a merged MR/PR"
      - "approval can satisfy an approval request but cannot satisfy a merge request"
      - "Verify the real artifact rather than a precursor"
      - "`verified`, `blocked`, `stale`, or `premise-changed`"

  - file: templates/skill/workflows/subagent-auxiliary.md
    must_contain:
      - "Optional dispatch unavailable while the task remains possible → continue inline"
      - "Never turn an unavailable optimization into a blocker"

  - file: templates/skill/workflows/subagent-driven.md
    must_contain:
      - "No primitive or optional dispatch denied → execute inline"

  - file: templates/skill/workflows/subagent-orchestration.md
    must_contain:
      - "## Degraded Mode"
      - "execute it inline from only that scope"

  - file: templates/skill/protocol-blocks/workflow-distillation-proposal.md
    must_contain:
      - "# Workflow Distillation Proposal"
      - "only after `Closure Complete`"
      - "two independently completed instances"
      - "Decline/defer creates no durable record"

  - file: docs/plans/README.md
    must_contain:
      - "# Implementation Plan Records And Archive"
      - "Plans are implementation-design and implementation-review contracts"
      - "consume a `requirement-ready` source; they do not own, complete, narrow, or"
      - "Requirement-bearing work first closes the desired outcome"
      - "through Requirement Definition"
      - "`implementation-ready`. Only then may a Plan derive technical means and proof"
      - "Complexity changes analysis depth, not materialization timing"
      - "Create the smallest current carrier"
      - "Use one dated file for a focused one-file record"
      - "Use a directory when the durable object is a named dossier"
      - "A correct dossier may contain only `prd.md`"
      - "Headings are not the completeness unit"
      - "Plan steps and test code cannot"
      - "returns to Requirement Definition"
      - "### Plan Composition"
      - "Can this Plan still independently complete, freeze, and be directly consumed later?"
      - "one-way `consumes`"
      - "`subplan_of: prd.md`"
      - "one harness-native Plan and one active mainline"
      - "Promote an existing truthful owner into that dossier before creating a third Plan"
      - "## Database-Structure Changes"
      - "complete target-state DDL derived from the authoritative current schema"
      - "forward schema update SQL plus required DML/backfill/data correction"
      - "read-only verification SQL"
      - "executable rollback SQL when reversal is proven safe"
      - "artifact does not itself authorize a database write"
      - "When the Plan has no database-structure impact, create no placeholder SQL file"
      - "final reconciliation -> done -> frozen"
      - "supersedes"
      - "Do not add `plan_role`, `components`, `consumed_by`, Component status mirrors, or subplan progress fields"
      - "initialize truthful `distilled_to:`"
      - "Purely technical Plans create no business leaf"
      - "One destination owns the complete definition"
    must_not_contain:
      - "it goes in both places"
      - "canonical Plan Skeleton"
      - "Complex plan — directory"

  - file: docs/plans/_TEMPLATE.md
    must_contain:
      - "Use this template only after `plan-feature.md` § Artifact Pressure Gate admits a durable Plan"
      - "Do not pre-create sibling files"
      - "Consume a `requirement-ready` source and proven Implementation Binding before deriving tasks"
      - "## Governing Requirement"
      - "Do not add or resolve normative meaning here"
      - "## Current Implementation Facts"
      - "Keep desired meaning in the governing Requirement"
      - "## Implementation Decisions"
      - "is not a Plan decision and must return to Requirement Definition"
      - "## Requirement Input And Proof Mapping"
      - "Proof code and commands are subordinate to acceptance"
      - "## Technical Open Questions"
      - "Task Interfaces: derive only after implementation-ready"
      - "A true subplan is not an independent Plan"
      - "Durable Integrating Plan"
      - "Database-structure change"
      - "The SQL artifact never grants DB-write authority"

  - file: templates/skill/workflows/maintain-docs.md
    must_contain:
      - "## Semantic Ownership and Local Actionability"
      - "business-truth owner inversion"
      - "one complete semantic owner"
      - "smallest task-specific trigger, immediate action, and current-task acceptance responsibility"
      - "A complete repeated rule or workflow is exceptional"
      - "independently delivered or independently used"
      - "### Source-Preserving User Input"
      - "user wording is source evidence, not draft prose"
      - "If the wording is already clear, keep it unchanged"
      - "must never become the sole normative record"

  - file: REFERENCE.md
    must_contain:
      - "Defining SBA product direction or whether to absorb a major capability"
      - "only after the Requirement is ready"
      - "Requirement decision handoff and closure"
      - "Decision capture: [`define-requirement.md`]"
      - "runtime authority/readiness: [`change-contract.md`]"
      - "desired truth vs implementation fact"
      - "implementation drift"
      - "Plans and other consumers keep only provenance, immediate action, and acceptance-to-proof hooks"

  - file: templates/skill/protocol-blocks/source-localization.md
    must_contain:
      - "complete technical narrowing contract"
      - "one Session-only runtime map"
      - "## Adaptive Evidence Funnel"
      - "Stages 1-2 expose no source bodies"
      - "Stage 4 is the first selected-source read"
      - "cannot rewrite desired meaning"
      - "ordinary localization creates no repository artifact"

  - file: templates/skill/protocol-blocks/change-contract.md
    must_contain:
      - "sole semantic owner of the runtime Requirement Contract and its later"
      - "Evaluate the Requirement Contract before implementation planning or mutation"
      - "A clear request, confirmed Requirement/issue/PRD, or accepted decision may"
      - "locality, estimate, elapsed time, file count, keywords, or complexity never"
      - "Every material requirement-bearing input receives an evidenced disposition"
      - "inference may generate candidates but cannot create authority"
      - "These dimensions are conditional, never a fixed questionnaire"
      - "`requirement-ready` means no remaining user-owned choice can redirect"
      - "Agent-discoverable files, symbols, callers"
      - "Bind Current -> Target without changing the Requirement"
      - "`implementation-ready` requires the still-valid Requirement Contract"
      - "A final file list, line-level design, exhaustive call graph"
      - "creates the Task Anchor after `requirement-ready`, but creates the Native Plan"
      - "only after `implementation-ready`; the Plan consumes rather than defines the"
      - "ordinary tasks create no contract file, ledger, Wiki, manifest, index, database"
    must_not_contain:
      - "## Adaptive Evidence Funnel"
      - "Search deterministically"

  - file: templates/skill/workflows/plan-feature.md
    must_contain:
      - "change-contract.md"
      - "source-localization.md"
      - "Current -> Target"
      - "must not redefine Goal, scope, model/flow, rules"
      - "not the Requirement Definition owner"
      - "A durable Plan names and consumes its governing Requirement"
    must_not_contain:
      - "## Question Gate"
      - "## User-Confirmed Decision Mainline"
      - "## Progressive Brainstorm Handoff"
      - "canonical owner of Decision Delta"

  - file: templates/skill/workflows/fix-bug.md
    must_contain:
      - "Reuse a clear observed/expected contract as the fast path"
      - "source-localization.md"

  - file: templates/skill/workflows/change-managed.md
    must_contain:
      - "A feature or any request that adds or changes user-visible behavior, a business flow/state, or an external contract is Managed even when it sounds local"
      - "complete Requirement Definition"
      - "before mutation"
      - "A clear source Requirement or explicit request may satisfy `requirement-ready` with zero questions"
      - "Only then mark `implementation-ready`, derive the Native Plan"
      - "Steps 1-3 must bind the returned owner, Current -> Target, affected path"

  - file: templates/skill/protocol-blocks/ambiguous-request-gate.md
    must_contain:
      - "identify whether the blocker is normative"
      - "instead of asking the user"

  - file: templates/skill/workflows/task-execution.md
    must_contain:
      - "Requirement-bearing changes always evaluate [`change-contract.md`](../protocol-blocks/change-contract.md)"
      - "clear input may satisfy `requirement-ready` immediately without questions"
      - "Fixed-contract Simple tasks keep the direct fast path"
      - "source-localization.md"

  - file: templates/skill/workflows/task-closure.md
    must_contain:
      - "final expanded [`Change Contract`](../protocol-blocks/change-contract.md)"
      - "does not redefine Requirement meaning, source-localization stages, read breadth, or proof depth after green"

  - file: templates/skill/scripts/check-version-conformance.sh
    must_contain:
      - "must_contain_in_order"
      - "ORDER_START"
      - "OUT OF ORDER"
      - "def decode_scalar("
      - "must be a non-empty block sequence"
      - "flow-style sequences are unsupported"
      - "except (OSError, UnicodeError)"
      - "ordered check error"

  - file: scripts/check-validation-contract.sh
    must_contain:
      - "check_ordered_precondition_contract"
      - "check_independent_verified_failure_classes"
      - "check_requirement_plan_separation_and_bounded_execution_contract"
      - "check_requirement_materialization_matrix"
      - "requirement/Plan/execution mutation accepted $mutation"
      - "Requirement Definition, Implementation Plan separation, bounded downshift, and terminal readback are independently mutation-protected"
      - "direct, Folder-light, routed, Plan, and broad carriers preserve fitted requirement/Plan separation without broken references"
      - "verified failure class mutation accepted missing $mutation"
      - "activation and learning-closure failures retain independent proof and completion boundaries"
      - "ordered manifest did not decode quoted YAML scalars or honor a later file key"
      - "ordered manifest accepted $schema_case without an ordered assertion"
      - "ordered scan error did not reach a stable failure summary"
      - 'cd "$primary" && bash "$smoke" . --phase 7'
      - "check_html_comments_are_not_live_references"
      - "check_namespaced_path_escapes_are_rejected"
      - "check_registration_owners_do_not_cross_resolve"
      - "check_required_files_path_escape_is_rejected"
      - "check_routing_comments_do_not_seed_reachability"
      - "check_github_slug_preserves_consecutive_hyphens"
      - "check_reason_first_required_files_mapping"
      - "check_many_markdown_files_do_not_hit_arg_max"
      - "check_harness_relative_skill_paths_resolve"
      - "Validation contract results: %s passed, %s failed"

# Files required to exist (presence-only check). Drift here means a
# self-hosting structural piece was deleted without updating routing.
required_files:
  - path: README.md
  - path: REFERENCE.md
  - path: EXAMPLES.md
  - path: workflows/upgrade-downstream.md
  - path: references/self-hosting-routing.yaml
  - path: references/business-global-model.md
  - path: references/permission-model.md
  - path: references/tests-as-spec.md
  - path: templates/skill/protocol-blocks/composite-plan-execution.md
  - path: templates/skill/protocol-blocks/user-decision-drift.md
  - path: templates/skill/protocol-blocks/plan-knowledge-closure.md
  - path: templates/skill/protocol-blocks/external-delivery-verification.md
  - path: templates/skill/protocol-blocks/workflow-distillation-proposal.md
  - path: templates/skill/conformance.yaml
  - path: templates/skill/scripts/check-version-conformance.sh
  - path: templates/skill/workflows/task-execution.md
  - path: templates/skill/workflows/update-rules.md
  - path: templates/skill/workflows/rule-update/business-truth.md
  - path: templates/skill/workflows/rule-update/verified-failure.md
  - path: templates/skill/workflows/rule-update/knowledge-reconciliation.md
  - path: templates/skill/workflows/rule-update/workflow-distillation.md
  - path: templates/skill/protocol-blocks/source-localization.md
  - path: templates/skill/protocol-blocks/change-contract.md
  - path: docs/task-anchor-native-plan.md
  - path: docs/sba-bible.md
