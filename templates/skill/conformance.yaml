# Content contract for the complete reusable scaffold, not a universal file
# inventory for every valid downstream Skill. Evidence-selected Single-file or
# Folder-light results use only a fitted manifest when their materialized owners
# have repeatable machine-check pressure.
# Validated by scripts/check-version-conformance.sh. Keep only load-bearing
# behavior anchors here; routing, generated blocks, links, and budgets have
# their own checks.

required_sections:
  - file: routing.yaml
    must_contain:
      - "id: receiving-review"
      - "workflow: workflows/receiving-review.md"
      - "reviewer 提了几个问题,处理一下"

  - file: protocol-blocks/ambiguous-request-gate.md
    must_contain:
      - "Vague wording is a signal, not an automatic blocker"
      - "smallest read-only evidence set"
      - "normative product or business preference"
      - "Do not mutate until the requested outcome is concrete enough to verify"
      - "User-as-index"
    must_not_contain:
      - "Let me scan the project first, then ask"
      - "**Stop.**"

  - file: workflows/task-execution.md
    must_contain:
      - "## Task Classifier"
      - "Classify by whether an explicit goal or requirement changes execution"
      - "one clear read-only or fixed-contract maintenance action"
      - "no new desired behavior/contract"
      - "a `requirement-ready` addition/change to user-visible behavior, business flow/state, or external contract"
      - "unresolved desired goal, scope, model/flow, rule/constraint, permission, preservation, or acceptance"
      - "a technical architecture choice between implementations that preserve a ready Requirement"
      - "Apparent code locality never makes requirement-bearing work Simple"
      - "reclassify before more mutation"
      - "build only the remaining Native Plan rather than asking the user to restate the task"
      - "## Task Anchor"
      - "After `requirement-ready` and before implementation-binding discovery"
      - "project the observable Goal"
      - "After source localization makes the Change Contract `implementation-ready`"
      - "derive the Native Plan"
      - "cannot infer or edit the Requirement"
      - "material acceptance/risk to fitted evidence"
      - "Technical evidence that changes only Current facts"
      - "without editing the Anchor"
      - "returns to Requirement Definition; only after the authoritative Requirement is"
      - "updated and reaches `requirement-ready` again"
      - "the result that would select the next read"
      - "Ranked, sampled, truncated, or Top-N output is candidate evidence, not an exhaustive inventory"
      - "## Workflow Boundary"
      - "## Recitation Loop"
      - "## Execution Loop"
      - "## New Message Gate"
      - "Already-clear wording stays verbatim"
      - "an Agent-derived summary cannot be the only normative record"
      - "## Exit To Closure"
      - "harness's native Plan/Task surface"
      - "do not repeat its steps in chat"
      - "## Presentation Gate"
      - "Evaluate **Structured Brief first**"
      - "Compact Alignment is allowed only when all Structured conditions are false"
      - "first user-facing task-start message MUST begin"
      - "Steps must be numbered"
      - "do not collapse the brief into prose or render empty headings"
      - "Before each main Plan step"
      - "Never silently overwrite a load-bearing conclusion"
      - "business owner's confirmation"
      - "every governing Requirement and Plan path"
      - "Only business-bearing decisions require a routed business rule"
      - "## User Decision Drift Gate"
      - "product meaning, business rule, scope, flow, state, boundary, invariant, acceptance condition, or user-visible behavior"
      - "Ordinary implementation choices that preserve those decisions do not trigger this gate"
      - "pause the affected path immediately"
      - "user-decision-drift.md"
      - "including updating the Task Anchor and remaining Native Plan before more mutation"
      - "the evidence that could resolve or falsify it"
      - "receiving a successful exit code"
      - "If it contradicts the premise"
      - "if it is inconclusive"
      - "do not narrate it before every tool call"
      - "does not create durable planning files or recover task state across Sessions"
      - "## Verified Failure Handoff"
      - "Session-only failure candidate"
      - "Retries inside the same unresolved task remain one occurrence"
      - "rule-update/verified-failure.md#verified-failure-input"
      - "Verified Failure owns durable reconciliation"
      - "every verified failure has a Rule Update learning outcome"
      - "If technical evidence changes only the implementation premise or ordering"
      - "do not edit the Anchor"
      - "Requirement Definition and re-project the Anchor only after the Requirement"
    must_contain_in_order:
      - "After `requirement-ready` and before implementation-binding discovery"
      - "After source localization makes the Change Contract `implementation-ready`"
      - "derive the Native Plan"
    must_not_contain:
      - "task_plan.md"
      - "cross-tool state sync"
      - "Plan: <concise task-specific steps"
      - "Re-anchor only when the user or evidence changes the Requirement frame"
      - "If evidence changes the premise, scope, ordering, or Done When, update the Anchor"

  - file: protocol-blocks/composite-plan-execution.md
    must_contain:
      - "# Composite Plan Execution"
      - "one truthful Integrating owner"
      - "`done` is frozen"
      - "missing, `abandoned`, replaced, or unauthorized inputs block"
      - "Component meaning, invariant, boundary, accepted interface"
      - "one Integrating Task Anchor and one Native Plan"
      - "Session-only composition creates no durable graph artifact"

  - file: protocol-blocks/user-decision-drift.md
    must_contain:
      - "# User Decision Drift"
      - "product meaning, business rule, scope, flow, state, boundary, invariant, acceptance condition, or user-visible behavior"
      - "Ordinary implementation choices that preserve those decisions do not trigger it"
      - "Code may expose constraints, cost, and risk"
      - "Keep the affected path paused"
      - "Mark every alternative `proposed`"
      - "Source-Preserving User Input"
      - "keep clear wording verbatim"
      - "label any Agent-derived summary as subordinate"
      - "bind every short answer to its original question/options"
      - "speech act/force, actor/ownership, timing/commitment, uncertainty/emphasis"
      - "definition/identity, applicability/preconditions, boundary/counterexample, negation/rejected direction, rationale/consequence, authority, scope"
      - "`proposed | confirmed | superseded` status"
      - "in the governing Requirement mainline"
      - "retain the source wording or ask the user"
      - "mark the prior decision `superseded`"
      - "Reconcile any routed business rule"
      - "re-project the Task Anchor, rebind implementation facts, and replace the remaining Native Plan before more mutation"
      - "Never silently compromise a confirmed mainline"
      - "A late disclosure in review or Closure never substitutes"

  - file: workflows/define-requirement.md
    must_contain:
      - "# Requirement Definition Workflow"
      - "produces `requirement-ready` and, only under its own"
      - "Artifact Pressure Gate, the Governing Requirement artifact"
      - "## Intake And Evidence"
      - "Evaluate only the Requirement Contract dimensions activated by this request"
      - "product meaning for the user"
      - "### Clear Requirement"
      - "the source-faithful Requirement Contract and return `requirement-ready`"
      - "Do not require the user to reply with a ceremonial confirmation"
      - "Keep it Session-only unless the Artifact Pressure Gate below admits a Governing"
      - "### Incomplete Requirement"
      - "present the current understanding in concrete outcome terms"
      - "ask the minimum normative question needed"
      - "stop before implementation planning, Task Breakdown, or mutation"
      - "## Brainstorm Boundary"
      - "Discuss one load-bearing normative topic at a time"
      - "material risk or trade-off, the Agent's recommendation"
      - "When the Requirement is already clear, ask no question"
      - "ordinary implementation means belong to"
      - "## Decision Authority And Source Fidelity"
      - "Keep a clear user statement verbatim as `User source`"
      - "## Requirement Artifact Pressure Gate"
      - "update the smallest Governing Requirement only when the user explicitly asks"
      - "independent reader, review"
      - "Complexity, activity, implementation size, or a clear Requirement alone is not"
      - "real project code root and follow its local Requirement archive contract"
      - "Exclude Current -> Target implementation design"
      - "Requirement does not enter Plan Feature or authorize mutation"
      - "A later durable implementation Plan links to and consumes this owner one way"
      - "Plan Feature cannot copy, narrow, resolve, or update the Governing"
      - "## Handoff"
      - "Requirement Definition does not derive implementation steps"
    must_not_contain:
      - "## Technical Design Slices"
      - "Make one risk-sized change"
      - "run the repository-owned fitted validation command"
      - "Do not create a Requirement file, Task Anchor, or Plan here"
      - "Requirement-only PRD uses Plan Feature's Artifact Pressure Gate"

  - file: workflows/plan-feature.md
    must_contain:
      - "# Implementation Plan Workflow"
      - "not the Requirement Definition owner"
      - "## Deliverable Gate"
      - "only representative evidence and return findings/next decisions"
      - "materialize artifacts, build Task Breakdown, or mutate code"
      - "## Intake Sequence"
      - "Never close it with an implementation assumption"
      - "Project the Task Anchor"
      - "Prove implementation facts"
      - "Reach `implementation-ready`"
      - "derive technical design, Task Interfaces, proof implementation"
      - "Task Breakdown and mutation are forbidden before this state"
      - "## Complexity Gate"
      - "Complexity changes analysis depth, never file creation"
      - "## Technical Design Slices"
      - "question -> Current evidence -> technical decision -> impact/risk -> proof"
      - "preserve the governing Requirement"
      - "returns to Requirement Definition"
      - "## Proof Design"
      - "The proof implementation is subordinate to acceptance"
      - "an unstated expected result, return that question to Requirement Definition"
      - "## Artifact Pressure Gate"
      - "after `implementation-ready`, the harness-native"
      - "Complexity or activity alone is not pressure"
      - "A durable Plan names and consumes its governing Requirement"
      - "An explicit request to create or update only the Requirement/PRD stays in"
      - "Requirement Definition and never borrows this Gate"
      - "return to Requirement Definition to materialize that owner first"
      - "use separate dossier carriers"
      - "`prd.md` owns the Requirement and `design.md` owns the Implementation Plan"
      - "`design.md` declares `requirement: prd.md`"
      - "A Requirement-only dossier may stop at `prd.md`"
      - "A new implementation Plan never uses `prd.md` as its technical or lifecycle owner"
      - "## Database-Change Artifact Gate"
      - "Its `prd.md` Requirement,"
      - "`design.md` Plan, and SQL surface remain separate owners"
      - "## Plan Contract And Handoff"
      - "A durable implementation Plan is a revisable technical contract"
      - "must not redefine Goal, scope, model/flow, rules"
      - "Every proof traces to Requirement acceptance"
      - "Requirement-only artifact delivery never enters this workflow"
      - "without copying or modifying it"
      - "[workflow-state:planning]"
    must_contain_in_order:
      - "Consume the Requirement"
      - "Project the Task Anchor"
      - "Prove implementation facts"
      - "Reach `implementation-ready`"
    must_not_contain:
      - "## Question Gate"
      - "## User-Confirmed Decision Mainline"
      - "## Progressive Brainstorm Handoff"
      - "canonical owner of Decision Delta"
      - "automatically enter a detailed user-facing brainstorm"
      - "docs/sba-bible.md"
      - "Pervasive reverse-question — default habit"
      - "one per lens"
      - "Record at least two real options"
      - "Create only `prd.md` initially"
      - "create `docs/plans/YYYY-MM-DD-<slug>/prd.md`"
      - "or a dossier `prd.md` only when it already needs independently readable"
      - "otherwise `prd.md` or the existing one-file Plan"
      - "plan_role"
      - "consumed_by"
      - "component_status"
      - "integration_status"
      - "Requirement-only PRD uses Plan Feature's Artifact Pressure Gate"

  - file: workflows/plan-large.md
    must_contain:
      - "## Multi-Perspective Analysis"
      - "Large changes analysis depth, not artifact count"
      - "a perspective is not a file"
      - "A suspected risk or consumer list is a candidate queue"
      - "## Angle Contract"
      - "Keep tightly coupled angles together"
      - "If an angle would be read only with every sibling"
      - "Never create one file per lens"
      - "## Risk Scenarios"
      - "Eventual consistency"
      - "Concurrency"
      - "Performance"
      - "Migration/compatibility"
      - "## Synthesis"
      - "conversation when no artifact is justified"
      - "or `design.md` when a durable Requirement dossier exists"
      - "`prd.md` remains the Governing Requirement and never becomes the Large Plan synthesis"
      - "## Parallel Analysis"
      - "bounded evidence region, expected result, and stop condition"
      - "Do not dispatch broad “scan the subsystem” work before that boundary"
    must_not_contain:
      - "Each selected lens gets a naturally named sibling file"
      - "Every angle file"
      - "otherwise `prd.md` or the existing one-file Plan"

  - file: workflows/task-closure.md
    must_contain:
      - "## Task Closure Protocol"
      - "### Entry Gate"
      - "return to [`task-execution.md`](task-execution.md)"
      - "final Anchor Checkpoint"
      - "restate the governing Requirement projection and Task Anchor"
      - "Plan-selected proof set, escalation signals, stop point"
      - "does not redefine Requirement meaning, source-localization stages, read breadth, or proof depth after green"
      - "### Rationalizations to Reject"
      - "### Red Flags"
      - "## After-Action Review"
      - "Test count is not evidence quality"
      - "recorded red reproduction → same-case green proof"
      - "remaining regression uncertainty"
      - "historical red evidence remains valid"
      - "plan-knowledge-closure.md"
      - "durable knowledge migrated/deleted/superseded"
      - "rather than creating a mandatory ledger"
      - "durable instruction or user-meaning compression"
      - "Semantic Before/After Reconciliation"
      - "decision-changing clause"
      - "Ready for Delivery"
      - "is a checkpoint, not task completion"
      - "Closure never grants commit, push, MR, deploy, publish"
      - "external-delivery-verification.md"
      - "session-only requested artifact set"
      - "every requested artifact only against that bound set"
      - "Closure Complete"
      - "The command exited 0, so the stage is complete"
      - "proves only the command's own contract"
      - "treating that exit code as sufficient completion evidence by itself"
      - "database-structure change"
      - "SQL-file existence alone is neither schema readiness nor DB delivery"
      - "Account for verified failures"
      - "rule-update/verified-failure.md` § Verified Failure Input"
      - "Closure does not diagnose root cause or postpone the first write"
      - "Do not re-record an already-reconciled verified failure"
      - "verified failure that lacks a completed learning outcome"
      - "### Post-Completion Workflow Distillation"
      - "workflow-distillation-proposal.md"
    must_not_contain:
      - "## Accepted Workflow-Distillation Input"

  - file: protocol-blocks/plan-knowledge-closure.md
    must_contain:
      - "# Plan And Knowledge Closure"
      - "enumerate every one-way `consumes` edge"
      - "read each Component from its canonical owner"
      - "A `done` Component is frozen"
      - "no required Component may remain `draft` or `executing`"
      - "a true `subplan_of` shares its parent's lifecycle"
      - "an independent Component retains its own lifecycle"
      - "Decision Delta, local acceptance, fresh proof, delivery responsibility, and durable distillation"
      - "Replay each relevant user-confirmed Requirement Decision Context"
      - "desired business truth"
      - "current implementation fact"
      - "Plan Leaf Reconciliation before implementation handoff"
      - "preserve one-way Requirement Provenance"
      - "consume rather than redefine that meaning"
      - "it is not the first place to compress or write the user-confirmed mainline"
      - "Run Knowledge Reciprocity"
      - "integrated acceptance, and delivery responsibility"
      - "Calibrate `distilled_to:`"
      - "cannot declare readiness or completion"

  - file: protocol-blocks/external-delivery-verification.md
    must_contain:
      - "# External Delivery Verification"
      - "never grants it or declares task completion"
      - "## Requested Artifact Set"
      - "Historical tasks, adjacent repositories or systems"
      - "create no ledger, database, or persistent status file"
      - "Before answering an aggregate status question"
      - "a pushed branch is not an MR/PR"
      - "an approved MR/PR is not a merged MR/PR"
      - "approval can satisfy an approval request but cannot satisfy a merge request"
      - "Verify the real artifact rather than a precursor"
      - "authoritative schema/data readback"
      - "Return `stale` when source content, branch/config inputs, or relevant verification changed"
      - "Return `premise-changed` when the requested artifact, target, authority, or delivery scope/premise changed"
      - "Retry only the external boundary"
      - "`verified`, `blocked`, `stale`, or `premise-changed`"

  - file: protocol-blocks/workflow-distillation-proposal.md
    must_contain:
      - "# Workflow Distillation Proposal"
      - "only after `Closure Complete`"
      - "two independently completed instances"
      - "frequency, adjacency, and command similarity do not qualify"
      - "`compose`"
      - "present no proposal"
      - "Decline/defer creates no durable record"
      - "Accepted Workflow-Distillation Input"

  - file: workflows/update-rules.md
    must_contain:
      - "compact shared durable-write contract plus the ambiguous-request dispatcher"
      - "## Shared Durable-Write Contract"
      - "### Input and Destination Classification"
      - "rule-update/business-truth.md"
      - "rule-update/verified-failure.md"
      - "rule-update/knowledge-reconciliation.md"
      - "rule-update/workflow-distillation.md"
      - "### Source, Authority, and Fidelity"
      - "### Reconciliation Gate"
      - "### Activation Check"
      - "### Placement and Generalization"
      - "### Synchronization and Verification"
      - "## Ambiguous Request Dispatcher"
      - "Select exactly one execution owner"
      - "No authority expanded"
    must_not_contain:
      - "## Business Leaf and Gotcha Ownership"
      - "## Verified Failure Input"
      - "## Ordinary Recording Threshold"
      - "## Accepted Workflow-Distillation Input"

  - file: workflows/rule-update/business-truth.md
    must_contain:
      - "Shared Durable-Write Contract"
      - "## Business Leaf and Gotcha Ownership"
      - "A Gotcha is not a general implementation reference or a second business-model owner"
      - "## Requirement Decision Source"
      - "define-requirement.md#decision-authority-and-source-fidelity"
      - "change-contract.md#requirement-contract"
      - "An implementation Plan carries only one-way Requirement Provenance"
      - "## Standalone Business-Model Input"
      - "create or reuse one draft gap dossier"
      - "## Active Bug-Fix Input"
      - "only possible business source is the user's explicit correction"
      - "Do not derive `desired business truth` from Bug symptoms"
      - "current implementation fact"
      - "## Business-Specific Completion"
    must_not_contain:
      - "`covered/no-write`"
      - "## Ordinary Recording Threshold"
      - "## Accepted Workflow-Distillation Input"

  - file: workflows/rule-update/verified-failure.md
    must_contain:
      - "Shared Durable-Write Contract"
      - "## Verified Failure Input"
      - "**Activation failure** - applicable prevention already exists"
      - "**Learning-closure failure** - the first occurrence has already proved a stable prevention"
      - "Their evidence is not interchangeable"
      - "Evaluate and close both classes separately whenever both are present"
      - "Stable execution preconditions need an earlier placement than their failure explanation"
      - "before its first protected command"
      - "count tool initialization as the intended red test"
      - "project-specific versions and commands stay downstream"
      - "first proven actionable failure bypasses only the ordinary Recording Threshold"
      - "`covered/no-write`"
      - "`extend-existing`"
      - "`correct-existing`"
      - "`add-and-activate`"
      - "`transient/no-write`"
      - "`requires-user-authority`"
      - "`escalate-prevention`"
      - "same proven root cause + same applicability boundary + same prevention action"
      - "repair activation instead of adding prose"
      - "Failure learning never grants commit, push, MR, deploy, publish, database, production, or other external authority"
      - "## Failure-Specific Completion"
      - "Activation failure and learning-closure failure were evaluated independently"
      - "one correct run was not used as proof of durable learning"
    must_contain_in_order:
      - "Stable execution preconditions need an earlier placement than their failure explanation"
      - "select the environment"
      - "verify what the command will actually use"
      - "before its first protected command"
    must_not_contain:
      - "## Business Leaf and Gotcha Ownership"
      - "## Ordinary Recording Threshold"
      - "## Accepted Workflow-Distillation Input"

  - file: workflows/rule-update/knowledge-reconciliation.md
    must_contain:
      - "Shared Durable-Write Contract"
      - "## Ordinary Recording Threshold"
      - "at least two must pass"
      - "## Upgrade Plan Gate"
      - "## When Not to Record"
      - "## Learn, Correct, Retire"
      - "correct in place and check consumers"
      - "repair activation/prominence instead of duplicating it"
      - "maintain-docs.md"
      - "## Knowledge-Specific Completion"
    must_not_contain:
      - "## Business Leaf and Gotcha Ownership"
      - "`covered/no-write`"
      - "## Accepted Workflow-Distillation Input"

  - file: workflows/rule-update/workflow-distillation.md
    must_contain:
      - "Shared Durable-Write Contract"
      - "## Accepted Workflow-Distillation Input"
      - "at least two independently completed semantic instances"
      - "**`extend`**"
      - "**`compose`**"
      - "**`create`**"
      - "Create one complete orchestration owner"
      - "source-only change without required downstream consumption is incomplete"
      - "generated or installed copies are never independent owners"
      - "return `requires-user-authority` without mutation"
      - "Workflow acceptance never grants commit, push, MR, deploy, publish, database"
      - "## Workflow-Specific Completion"
    must_not_contain:
      - "## Business Leaf and Gotcha Ownership"
      - "`covered/no-write`"
      - "## Ordinary Recording Threshold"

  - file: workflows/profile-business-model.md.example
    must_contain:
      - "Reconcile leaf / Gotcha ownership"
      - "Lift stable business truth that exists only in a Gotcha into the leaf"
      - "## User-Confirmed Input"
      - "define-requirement.md"
      - "Requirement Decision Delta owned by Requirement Definition"
      - "Implementation-Fact Question Gate"
      - "Close every decision-relevant trigger/entry"
      - "trace distinct flows independently"
      - "Cross-check that evidence with existing user-confirmed answers"
      - "accept the point and do not ask again"
      - "Never ask the user to choose an interface, branch, write target, protection rule, call order, or other current implementation fact"
      - "Standalone modeling writes stable, cross-implementation meaning directly into the routed leaf"
      - "does not create a PRD"
      - "## Confirmed Implementation Gap Collection and Handoff"
      - "reuse an authoritative Governing Requirement for the same scope"
      - "separate `design.md` consumer instead of putting technical Plan content in `prd.md`"
      - "never create one PRD per gap"
      - "Continue the user's agreed modeling scope"
      - "Plan Feature may write implementation design only to the separate Plan carrier"
      - "keep the Requirement draft and stop"
      - "show the deduplicated gap set"
      - "The default unit is one business domain, not one code module"
      - "about 120 nonblank lines"
      - "more than 10 substantive H2 sections"
      - "Size only triggers review; it never decides the split"
      - "independently understandable business submodules"
      - "Do not split by document aspect"
      - "The index must select a leaf from task signals and carry no business body"
      - "rather than define an abstract concept from scratch"
      - "Authority: user-confirmed"
      - "desired business truth"
      - "current implementation fact"
      - "Persist by task type"
      - "immediate responsibility to pause the affected path"
      - "task-execution.md#user-decision-drift-gate"
      - "Do not ask code and the confirmed model to compete for authority"
      - "--workspace-root <workspace-root>"
      - "plain `scripts/sync-routing.sh --check` is sufficient only when every owner is skill-local"

  - file: scripts/sync-routing.sh
    must_contain:
      - "domain_overlays"
      - "domain overlay must inherit the task workflow"
      - "owner_roots"
      - "owner_ref_pattern"
      - "--workspace-root"
      - "task route does not preload knowledge"
      - "workflow must select a workflows/*.md procedure"
      - "Keywords identify candidates only"
      - "cross-owner target verification remains open"
      - "code-root target verification remains open"
      - "code target missing"
    must_not_contain:
      - "allowed_apps"

  - file: scripts/smoke-test.sh
    must_contain:
      - 'REFERENCE_INPUT="$(mktemp)"'
      - 'file_list.read_bytes().split(b"\0")'
      - "optional_cue_re = re.compile("
      - 'return re.sub(r"<!--.*?-->", "",'
      - "def slugify_heading(value: str) -> str:"
      - "def owner_root(src: Path) -> Path:"
      - "root = owner_root(src)"
      - 'plain.startswith("skills/") and under(src, skill_root)'
      - "skill_registry_root = ("
      - "base = skill_registry_root"
      - "path escapes"

  - file: scripts/check-version-conformance.sh
    must_contain:
      - "required_files items must be mappings"
      - "must_contain_in_order"
      - "ORDER_START"
      - "OUT OF ORDER"
      - "def decode_scalar("
      - "must be a non-empty block sequence"
      - "flow-style sequences are unsupported"
      - "except (OSError, UnicodeError)"
      - "ordered check error"
      - "records = parse(path)"
      - "target.relative_to(skill_root)"
      - "owner path escapes the skill root"

  - file: scripts/audit-orphans.sh
    must_contain:
      - 'owner.rglob("*.md")'
      - "cross_sources"

  - file: scripts/route-reachability.sh
    must_contain:
      - "ACTIVE_DIRS=(architecture conventions gotchas rules workflows references)"
      - 'owner.rglob("*.md")'
      - "strip_yaml_comments"
      - "invalid_references"
      - "path escapes"
      - "code_from_skill"

  - file: workflows/maintain-docs.md
    must_contain:
      - "## Semantic Ownership and Local Actionability"
      - "Business Leaf and Gotcha Ownership"
      - "business-truth owner inversion"
      - "smallest task-specific trigger, immediate action, and current-task acceptance responsibility"
      - "A complete repeated rule or workflow is exceptional"
      - "generation is justified when a target must be independently delivered or independently used"
      - "sentence quota, score, marker, or file-count target"
      - "## Step 2: Independent Load-Reason Audit"
      - "## Step 5: Semantic Before/After Reconciliation"
      - "For user-brainstorm input"
      - "### Source-Preserving User Input"
      - "user wording is source evidence, not draft prose"
      - "If the wording is already clear, keep it unchanged"
      - "A stop request, responsibility handoff, deadline/promise, confidence limit, or repeated emphasis is semantic content"
      - "must never become the sole normative record"
      - "proposed | confirmed | superseded"
      - "exact `owner#section`"
      - "normal activation timing"

  - file: workflows/fix-bug.md
    must_contain:
      - "## Mandatory Pre-Step"
      - "Design-or-defect gate"
      - "Repair-depth gate"
      - "invariant-owning boundary"
      - "smallest semantically complete repair"
      - "full/incremental/read/write path"
      - "user-confirmed Requirement Decision Context"
      - "Purely technical Design-derived work may consume the Plan without requiring a business model"
      - "define-requirement.md"
      - "Implementation-Fact Question Gate"
      - "close every decision-relevant trigger/entry"
      - "trace distinct flows independently"
      - "Never ask the user to choose a Current implementation fact"
      - "If the user says the answer is in code"
      - "cross-check it against any existing user-confirmed answer"
      - "continue without another question"
      - "ask only a remaining normative ambiguity that could change repair classification or expected behavior"
      - "Never ask the user to choose a Current implementation fact, request exhaustive detail, or define an abstract concept from scratch"
      - "rule-update/business-truth.md#active-bug-fix-input"
      - "instead of waiting for Task Closure"
      - "the only source eligible for a `references/business/<module>.md` record is the user's explicit business statement"
      - "Bug symptoms, root cause, repaired behavior, code, tests, runtime evidence, and Agent summaries are not `desired business truth`"
      - "rule-update/verified-failure.md#verified-failure-input"
      - "After Step 8 proves an engineering failure and repair"
      - "For each verified engineering failure, run Verified Failure Input now"
      - "does not defer the verified-failure write"
      - "every verified engineering failure received an immediate technical learning outcome through `workflows/rule-update/verified-failure.md` after red-to-green proof"
      - "No Bug-derived or Agent-derived content entered the business model"
      - "User Decision Drift Gate"
      - "task-execution.md#user-decision-drift-gate"
      - "Task Closure Protocol"
    must_not_contain:
      - "Pervasive reverse-question — default habit"
      - "directly `spawn_agent`"
      - "as candidate business knowledge"

  - file: protocol-blocks/ambiguous-request-gate.md
    must_contain:
      - "## Implementation-Fact Question Gate"
      - "close the smallest decision-relevant implementation path"
      - "never ask the user to choose them"
      - "Trace distinct flows independently"
      - "remaining normative ambiguity"

  - file: rules/change-discipline.md
    must_contain:
      - "## Semantic Boundary"
      - "Default to Product Development"
      - "project-rules.md"
      - "coding-standards.md"
      - "ownership/provenance"
      - "Dependency count is risk evidence"
      - "Use Operational Stabilization only"
      - "## Mutation Boundary"

  - file: workflows/task-execution.md
    must_contain:
      - "## Evidence and Knowledge Gate"
      - "smallest source/runtime slice"
      - "diagnostic evidence, never correctness gates"
      - "material Boundaries such as scope, non-goals"
      - "harness's native Plan"
      - "Present only useful alignment"
      - "do not repeat its steps in chat"
      - "Before each main Plan step"
      - "cross-cutting modifier, never a first-workflow route"
      - "task size or duration alone does not activate it"
      - "## Composite Plan Handoff"
      - "composite-plan-execution.md"
      - "Components never create competing runtime anchors"
      - "## Database-Structure Mutation Gate"
      - "dedicated Plan-local SQL sibling"
      - "never permission to bypass the project migration workflow or write a database"
    must_not_contain:
      - "locally_done"
      - "ready_for_parent"
      - "waiting_for_component"
      - "component_status"
      - "integration_status"
      - "plan_role"

  - file: workflows/subagent-auxiliary.md
    must_contain:
      - "## Admission Test"
      - "## Main-Agent Boundary"
      - "## Interception Transparency"
      - "Optional dispatch unavailable while the task remains possible → continue inline"
      - "Never turn an unavailable optimization into a blocker"

  - file: workflows/subagent-driven.md
    must_contain:
      - "## Mode Selection"
      - "## Mode 2 Entry"
      - "## Harness Compatibility"
      - "Worker-local status never advances the Native Plan"
      - "No primitive or optional dispatch denied → execute inline"
    must_not_contain:
      - "Iron Law (violation = task failure"
      - "must directly call `spawn_agent`"

  - file: workflows/subagent-orchestration.md
    must_contain:
      - "## 1. Contract"
      - "## 3. Main-Agent Review"
      - "Task Ref"
      - "Context Read"
      - "provenance index, not proof"
      - "does **not** run project-wide Task Closure"
      - "## Degraded Mode"
      - "execute it inline from only that scope"

  - file: protocol-blocks/subagent-contract.md
    must_contain:
      - "## Task Ref"
      - "## Role"
      - "## Evidence"
      - "Context Read"
      - "Files Changed"
      - "Checks Run"
      - "Remaining Risks"
      - "provenance index, not proof"

  - file: workflows/receiving-review.md
    must_contain:
      - "## Process"
      - "Verify every claim"
      - "governing Requirement Decision Context"
      - "Purely technical Design-derived work consumes the Plan without requiring a business model"
      - "pause the affected path and ask the user immediately"
      - "task-execution.md#user-decision-drift-gate"

  - file: workflows/update-upstream.md
    must_contain:
      - "## Hard Rules"
      - "## Procedure"
      - "conformance.yaml"
      - "Vendor: only paths explicitly listed"
      - "Project-owned: every non-vendor source file"
      - "Generated: Always Read, Common Tasks action hook"
      - "Runtime-data: hook state, caches, secrets"

  - file: workflows/change-managed.md
    must_contain:
      - "Map semantic fan-out"
      - "full/incremental/read/write path"
      - "smallest semantically complete change"
      - "Replay the Requirement and bound the owner region"
      - "carry its Goal, acceptance, material Boundaries, authority, and preserved behavior"
      - "User Decision Drift Gate"
      - "task-execution.md#user-decision-drift-gate"
    must_not_contain:
      - "Pervasive reverse-question — default habit"
      - "directly `spawn_agent`"

  - file: workflows/refactor-fanout.md
    must_not_contain:
      - "batch by physical region anyway to enable parallel dispatch"
      - "Full test suite passes"

  - file: protocol-blocks/source-localization.md
    must_contain:
      - "This file owns the complete technical narrowing contract"
      - "skip directly to a selected-source check when the target is already proven concrete"
      - "one Session-only runtime map"
      - "## Adaptive Evidence Funnel"
      - "Search deterministically"
      - "Stages 1-2 expose no source bodies"
      - "Stage 4 is the first selected-source read"
      - "a path, lexical match, file read, or successful command is not owner proof"
      - "Source evidence may falsify a Current premise or expose a Requirement conflict"
      - "ordinary localization creates no repository artifact"

  - file: protocol-blocks/change-contract.md
    must_contain:
      - "sole semantic owner of the runtime Requirement Contract and its later"
      - "Evaluate the Requirement Contract before implementation planning or mutation"
      - "A clear request, confirmed Requirement/issue/PRD, or accepted decision may"
      - "locality, estimate, elapsed time, file count, keywords, or complexity never"
      - "Every material requirement-bearing input receives an evidenced disposition"
      - "inference may generate candidates but cannot create authority"
      - "These dimensions are conditional, never a fixed questionnaire"
      - "`requirement-ready` means no remaining user-owned choice can redirect"
      - "Agent-discoverable files, symbols, callers"
      - "Bind Current -> Target without changing the Requirement"
      - "`implementation-ready`"
      - "A final file list, line-level design, exhaustive call graph"
      - "creates the Task Anchor after `requirement-ready`, but creates the Native Plan"
      - "only after `implementation-ready`; the Plan consumes rather than defines the"
      - "ordinary tasks create no contract file, ledger, Wiki, manifest, index, database"
    must_not_contain:
      - "## Adaptive Evidence Funnel"
      - "Search deterministically"

  - file: workflows/plan-feature.md
    must_contain:
      - "change-contract.md"
      - "source-localization.md"
      - "Current -> Target"
      - "must not redefine Goal, scope, model/flow, rules"
    must_not_contain:
      - "Every material requirement-bearing input receives an evidenced disposition"
      - "## Adaptive Evidence Funnel"

  - file: workflows/fix-bug.md
    must_contain:
      - "Reuse a clear observed/expected contract as the fast path"
      - "change-contract.md"
      - "source-localization.md"
      - "must identify the repair target and root-cause path before mutation"
    must_not_contain:
      - "Every material requirement-bearing input receives an evidenced disposition"
      - "## Adaptive Evidence Funnel"

  - file: workflows/change-managed.md
    must_contain:
      - "A feature or any request that adds or changes user-visible behavior, a business flow/state, or an external contract is Managed even when it sounds local"
      - "complete Requirement Definition"
      - "before mutation"
      - "A clear source Requirement or explicit request may satisfy `requirement-ready` with zero questions"
      - "change-contract.md"
      - "source-localization.md"
      - "Steps 1-3 must bind the returned owner, Current -> Target, affected path"
    must_not_contain:
      - "Every material requirement-bearing input receives an evidenced disposition"
      - "## Adaptive Evidence Funnel"

  - file: protocol-blocks/ambiguous-request-gate.md
    must_contain:
      - "let [`change-contract.md`](change-contract.md) identify whether the blocker is normative"
      - "use [`source-localization.md`](source-localization.md) instead of asking the user"
      - "Owner inversion"
    must_not_contain:
      - "Every material requirement-bearing input receives an evidenced disposition"
      - "## Adaptive Evidence Funnel"

  - file: workflows/task-execution.md
    must_contain:
      - "Requirement-bearing changes always evaluate [`change-contract.md`](../protocol-blocks/change-contract.md)"
      - "clear input may satisfy `requirement-ready` immediately without questions"
      - "consume [`source-localization.md`](../protocol-blocks/source-localization.md)"
      - "Fixed-contract Simple tasks keep the direct fast path"
    must_not_contain:
      - "Every material requirement-bearing input receives an evidenced disposition"
      - "## Adaptive Evidence Funnel"

  - file: workflows/profile-project.md
    must_contain:
      - "apply the admitted executable-architecture contract"
      - "only when low freedom and repeated/high-error pressure both exist"
      - "prefer a structured API, existing CLI, existing script, then a new minimal script"
      - "Keep goals, semantic parameters, exceptions, authority, and escalation with the Agent"
      - "keep a one-off directly verifiable action on the direct path"
      - "authoritative re-readable result"
      - "run identity, provenance, freshness, completeness/semantic validity, and terminal state"
    must_not_contain:
      - "../../../references/executable-skill-architecture.md"

  - file: workflows/task-closure.md
    must_contain:
      - "final expanded [`Change Contract`](../protocol-blocks/change-contract.md)"
      - "Closure consumes that contract; it does not redefine Requirement meaning, source-localization stages, read breadth, or proof depth after green"
    must_not_contain:
      - "Every material requirement-bearing input receives an evidenced disposition"
      - "## Adaptive Evidence Funnel"

required_files:
  - path: routing.yaml
  - path: workflows/define-requirement.md
  - path: workflows/plan-feature.md
  - path: workflows/task-execution.md
  - path: workflows/update-rules.md
  - path: workflows/rule-update/business-truth.md
  - path: workflows/rule-update/verified-failure.md
  - path: workflows/rule-update/knowledge-reconciliation.md
  - path: workflows/rule-update/workflow-distillation.md
  - path: protocol-blocks/source-localization.md
  - path: protocol-blocks/change-contract.md
  - path: protocol-blocks/composite-plan-execution.md
  - path: protocol-blocks/user-decision-drift.md
  - path: protocol-blocks/plan-knowledge-closure.md
  - path: protocol-blocks/external-delivery-verification.md
  - path: protocol-blocks/workflow-distillation-proposal.md
